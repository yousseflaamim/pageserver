export interface BookDetail {
    title: string;
    author: string;
    quantity : number;
    purchased:number;
   
  }
  /**This code defines an interface called 
   * BookDetail. An interface in TypeScript is used
   *  to define the structure and types of an object
   * . In this case, the BookDetail interface defines
   *  the properties that a book object should have. */