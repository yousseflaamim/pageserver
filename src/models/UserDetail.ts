export default interface UserDetail {
  username: string;
  password: string;
  role: string;
}
/**This code defines an interface called 
   * BookDetail. An interface in TypeScript is used
   *  to define the structure and types of an object
   * . In this case, the userDetail interface defines
   *  the properties that a user object should have. */